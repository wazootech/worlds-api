services:
  worlds-api:
    build: .
    ports: ["8080:8080"]
    environment:
      - LIBSQL_URL=file:/data/worlds.db
      - WORLDS_ADMIN_KEY=wzp_dev_admin_key
    volumes:
      - worlds-data:/data

volumes:
  worlds-data: {}

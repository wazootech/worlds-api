services:
  worlds-api:
    build: .
    ports: ["8080:8080"]
    environment:
      - WORLDS_ADMIN_KEY=wzp_dev_admin_key
    # worlds-api storage is bound at deploy time (Cloudflare D1); the
    # container no longer accepts a Turso/LibSQL URL via environment.
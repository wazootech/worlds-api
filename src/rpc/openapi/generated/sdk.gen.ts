// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { RpcData, RpcErrors, RpcResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Unified RPC endpoint for Worlds.
 *
 * JSON body: discriminated `{ action, request }` per `WorldsRpcRequest` (OpenAPI). This is **not** [JSON-RPC 2.0](https://www.jsonrpc.org/specification): there is no `jsonrpc` version field, `id`, or `params`/`result` in the spec sense. Success: HTTP 200 with `{ action, response }`. RPC failures return `{ action, error: { code, message } }` with HTTP status derived from `error.code` (e.g. 404 for `NOT_FOUND`, 400 for `INVALID_ARGUMENT`, 500 for `INTERNAL`); classify failures with `error.code`, not HTTP status alone. With the default transport preset (see `src/rpc/transport`), CORS is enabled, JSON body size is capped (HTTP **413** when exceeded), and `/rpc` is rate limited (HTTP **429**); tune via env or TransportConfig. Auth is enforced via API keys — include `X-Api-Key` header on every request; missing/invalid keys return HTTP **401** (RPC code `UNAUTHENTICATED`), and unauthorized access returns HTTP **403** (RPC code `PERMISSION_DENIED`).
 */
export const rpc = <ThrowOnError extends boolean = false>(options: Options<RpcData, ThrowOnError>) => (options.client ?? client).post<RpcResponses, RpcErrors, ThrowOnError>({
    security: [{ name: 'X-Api-Key', type: 'apiKey' }],
    url: '/rpc',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
